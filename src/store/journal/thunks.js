export const startNewNote = () => {
    return async (dispatch) => {
        //uid
        const newNote = {
            title: '',
            body: '',
            date: new Date().getDate(),
        }

        //!Dispatch
        // dispatch(newNote)
        // dispatch(activarNote)
    }
}